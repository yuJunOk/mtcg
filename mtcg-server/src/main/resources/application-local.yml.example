# 复制为 application-local.yml 后按需修改（application-local.yml 已加入 .gitignore）
# cp application-local.yml.example application-local.yml

spring:
  datasource:
    username: postgres
    password: postgres

mtcg:
  jwt:
    # HS256 密钥至少 32 字节
    secret: local-dev-secret-key-change-me-32chars
