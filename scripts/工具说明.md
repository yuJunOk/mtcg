# 工具脚本说明

本目录存放 MTCG 项目的辅助工具脚本。

> ★ **补充卡牌素材请先看**：[卡牌素材补充指南](../docs/设计文档/卡牌素材补充指南.md)  
> （官网拉取 / 截图补 RP·推广包 / 统一 1488×2080 规格 / 命名与目录约定）

---

## 目录结构

```
scripts/
├── 工具说明.md              # 本文件
├── package.json             # npm 依赖（tesseract.js）
├── package-lock.json
│
├── card_common/            # ★ 卡面统一规格（去透明边 / 1488×2080）
│   ├── README.md
│   ├── spec.py
│   ├── normalize.py
│   └── normalize_cli.py
│
├── 卡面提取/               # 截图批量提取（RP/推广包等补齐）
│   ├── README.md           # 使用说明
│   ├── extract_card.py     # 核心提取脚本（Python）
│   ├── run_extract.bat     # 双击运行
│   └── fix_execution_policy.reg # PowerShell 执行策略修复（首次使用）
│
├── 官网卡表拉取/             # 官方卡表 API 拉取（JSON/SQL/卡图）
│   ├── README.md
│   ├── fetch_cards.py
│   └── out/                # cards.json / products.json / SQL 副本
│
└── 规则文档提取/             # 微信规则文章提取工具（Node.js）
    ├── README.md           # 工具说明
    ├── extract.js          # 解析微信文章 HTML
    ├── download.js         # 下载文章图片
    ├── ocr.js             # 中文 OCR 识别
    └── lib/               # 工具数据和产物
        ├── chi_sim.traineddata # tesseract 中文训练数据
        └── results/         # 中间产物（OCR 结果、元数据）
```

---

## 快速开始

### 1. 安装依赖

```bash
cd scripts && npm install
```

### 2. 准备微信文章

浏览器打开文章 → 右键「查看网页源代码」→ 全选复制 → 保存为 `.html` 文件

### 3. 提取文章内容

```bash
cd scripts/规则文档提取
node extract.js <文章.html>
```

### 4. 下载文章图片

```bash
node download.js <文章.html> <输出目录> [图片前缀] [格式]
```

### 5. OCR 识别（可选）

```bash
node ocr.js <图片目录> <输出文件>
```

---

## 官方更新流程

当官方发布新的规则文章时：
1. 保存网页源代码为 `.html` 文件
2. `node extract.js <文章.html>` 解析内容
3. `node download.js <文章.html> ../docs/规则文档/规则书图片 rules-` 下载图片
4. 整理规则文本，更新 `docs/规则文档/` 下对应的 `.md`
5. 更新版本号和来源链接

如果新文章是规则 Q&A：
1. `node download.js <文章.html> ../docs/规则文档/QA图片-第{N}期/ qa{N}-`
2. `node ocr.js ../docs/规则文档/QA图片-第{N}期/ lib/results/qa{N}_ocr.txt`
3. 人工校对，修正识别错误
4. 在 `docs/规则文档/` 下新建 `05-规则Q&A-第{N}期.md`
5. 更新索引
